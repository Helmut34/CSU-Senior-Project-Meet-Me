services:
  backend:
    build: ./backend
    env_file: .env
    environment:
      - FLASK_ENV=production
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
